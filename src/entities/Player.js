// src/entities/Player.js

import { Entity } from "./Entity.js";

/**
 * @class
 * Represents a player.
 */
export class Player extends Entity {
  /**
   * Create a player.
   */
  constructor(x, y) {
    super(x, y);
  }
}
